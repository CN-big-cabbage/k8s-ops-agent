{
  "name": "@openclaw/k8s",
  "version": "1.3.0",
  "description": "OpenClaw Kubernetes operations plugin",
  "type": "module",
  "dependencies": {
    "@kubernetes/client-node": "^0.21.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.1"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
