# k8s-gateway

Gateway API management: list/describe gateways, list/describe routes, list classes, health status.

Uses CustomObjectsApi for gateway.networking.k8s.io CRDs. Gracefully handles Gateway API not installed.
